{
  "name": "nasa-exoplanet-frontend",
  "version": "1.0.0",
  "description": "Frontend for NASA Exoplanet Classification Project",
  "scripts": {
    "dev": "next dev",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "next build",
    "build:backend": "echo 'Backend build completed - Python ML service ready for deployment'",
    "build:production": "npm run build && npm run verify:build",
    "verify:build": "echo 'Verifying production build...' && ls -la .next && echo '✅ Frontend build verified'",
    "start": "next start -p ${PORT:-3000}",
    "lint": "next lint",
    "prebuild": "echo '🚀 Starting production build process...'",
    "postbuild": "echo '✅ Production build completed successfully!'"
  },
  "dependencies": {
    "next": "^15.5.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-icons": "^5.5.0",
    "three": "^0.180.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
